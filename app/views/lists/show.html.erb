<div class="container">
  <h1> <%= @list.name %> </h1>

      <% @list.bookmarks.each do |bookmark| %>
        <div class="card">
          <div class="card-body" style="text-align: center">
        <%= bookmark.movie.title %> 
      </div>
      </div>

      <div class= "comment"><p>Content: <%= bookmark.comment  %> <span><%= link_to "DELETE" , bookmark_path(bookmark), method: :delete, class: "btn btn-primary" %></span> </p>
     
      </div>

        <%= link_to "" , bookmark_path(bookmark), method: :delete %>
      <% end  %>
 

  <div class="buttons">
    <%= link_to "GO BACK TO LISTS", lists_path, class: "btn btn-success"%>
    <%= link_to "ADD A MOVIE", new_list_bookmark_path(@list), class: "btn btn-primary"%>
    <%= link_to "Delete List", list_path(@list),method: :delete, data: { confirm: "Are you sure?" } , class: "btn btn-secondary" %>
  </div>
</div>
